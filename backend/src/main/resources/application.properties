# Server port - Changed to 8085 as requested
server.port=2000

# MySQL Database Configuration
# Replace 'your_mysql_user' and 'your_mysql_password' with your actual credentials
spring.datasource.url=jdbc:mysql://localhost:3306/art_gallery?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=@Nikhil1766
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- JWT SECRET CONFIGURATION ---
# IMPORTANT: This secret key is for development ONLY.
# Generate a secure, 64-byte, Base64-encoded key for production.
# Command to generate a new key (Linux/Mac): openssl rand -base64 64
app.jwt.secret=MySuperSecretAndInsecureKeyForDevelopmentPurposesOnlyChangeMePlease12345
app.jwt.expiration-ms=86400000 

# --- File Storage Configuration ---
# This is the relative path from the backend project root where images will be stored.
# Ensure the 'uploads' directory exists in the backend root.
file.upload-dir=./uploads